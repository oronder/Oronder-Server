# Copy this file to .env on your server (same directory as docker-compose.yml)
# Fill in all values. Do NOT commit your real .env file to git.

# --------------------
# Application runtime
# --------------------
# Discord bot token
DISCORD_TOKEN=
# Discord OAuth client secret (if your app does OAuth callbacks)
DISCORD_CLIENT_SECRET=
# Public base URL for your API (used in callbacks/links)
API_URL=https://api.example.com
# Log verbosity for the app (e.g., DEBUG, INFO, WARNING)
LOG_LEVEL=INFO

# --------------------
# Database (Postgres)
# --------------------
# These configure the Postgres container
POSTGRES_USER=oronder
POSTGRES_PASSWORD=change_me
POSTGRES_DB=oronder

# Connection string the app uses to reach Postgres
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@oronder-db:5432/${POSTGRES_DB}

# --------------------
# Backups (Backblaze B2)
# --------------------
B2_APPLICATION_KEY_ID=
B2_APPLICATION_KEY=
B2_BUCKET=
B2_NOTIFICATION_WEBHOOK=

# --------------------
# Uptime / GitHub API (optional)
# --------------------
GITHUB_UPTIME_PAT=

# --------------------
# Wiki.js integration  (optional)
# --------------------
WIKIJS_TOKEN=
WIKIJS_URL=https://wiki.example.com/graphql
